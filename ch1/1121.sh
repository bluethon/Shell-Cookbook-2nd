fname()
{
    echo $0;        # 脚本名称
    echo $1, $2;    # 访问参数1, 参数2
    echo "$@";      # 列表方式一次打印所有参数, "$1" "$2" "$3"
    echo "$*";      # 类似$@, 参数单个实体(不常用), "$1c$2c$3", c是IFS第一个字符
    return 0;       # 返回值
}